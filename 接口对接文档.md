# AI推荐云系统 - 前后端接口对接详细文档

## 1. 系统概述

AI推荐云系统是一个基于微服务架构的推荐系统平台，包含AI智能对话、商品管理、统计分析等多个功能模块。本文档详细说明了前端与后端各服务之间的接口对接规范。

## 2. 技术架构

### 2.1 前端技术栈
- React 19
- TypeScript
- Ant Design v5
- Axios
- Vite 7+

### 2.2 后端技术栈
- Spring Boot WebFlux (响应式编程)
- R2DBC (响应式数据库访问)
- 微服务架构

## 3. 接口规范

### 3.1 通用规范
- 所有API请求使用JSON格式
- 统一使用Bearer Token认证
- 响应格式统一为：
```json
{
  "code": 200,
  "msg": "success",
  "data": {}
}
```

### 3.2 API前缀
- 系统管理: `/api/sys-api`
- 商品管理: `/api/goods-api`
- AI服务: `/api/ai-api`
- 行为分析: `/api/behavior-api`

## 4. AI聊天服务接口

### 4.1 流式对话接口
- **请求路径**: `POST /api/ai-api/chat/fluxChat`
- **请求体**:
```json
{
  "prompt": "用户输入的问题",
  "sessionId": "会话ID(可选)",
  "userId": "用户ID(可选)"
}
```
- **响应**: SSE流式响应，每次返回一个结果片段
- **用途**: 实现AI助手的实时流式对话功能

### 4.2 一次性对话接口
- **请求路径**: `POST /api/ai-api/chat/chatAll`
- **请求体**: 同流式对话接口
- **响应**: 完整的对话结果
- **用途**: 获取完整对话结果

### 4.3 对话记录管理接口
- **获取对话记录**: `POST /api/ai-api/chat/findChat`
- **删除对话**: `DELETE /api/ai-api/chat/deleteById/{id}`
- **保存对话标题**: `PUT /api/ai-api/chat/saveChat`

## 5. AI管理服务接口

### 5.1 模型管理接口
- **保存/更新模型**: `POST /api/ai-api/bigModel/save`
- **删除模型**: `DELETE /api/ai-api/bigModel/deleteById/{id}`
- **分页查询模型**: `POST /api/ai-api/bigModel/findPage`

### 5.2 知识库管理接口
- **保存知识库**: `POST /api/ai-api/knowledge/save`
- **删除知识库**: `DELETE /api/ai-api/knowledge/deleteById/{id}`
- **上传文档**: `POST /api/ai-api/knowledge/uploadDoc`

## 6. 商品管理服务接口

### 6.1 商品管理接口
- **保存商品**: `POST /api/goods-api/product/save`
- **删除商品**: `DELETE /api/goods-api/product/deleteById/{id}`
- **分页查询商品**: `POST /api/goods-api/product/findPage`
- **根据ID查询商品**: `GET /api/goods-api/product/findById/{id}`

### 6.2 SKU管理接口
- **保存SKU**: `POST /api/goods-api/sku/save`
- **删除SKU**: `DELETE /api/goods-api/sku/deleteById/{id}`
- **分页查询SKU**: `POST /api/goods-api/sku/findPage`

### 6.3 分类管理接口
- **保存分类**: `POST /api/goods-api/category/save`
- **删除分类**: `DELETE /api/goods-api/category/deleteById/{id}`
- **分页查询分类**: `POST /api/goods-api/category/findPage`

### 6.4 标签管理接口
- **保存标签**: `POST /api/goods-api/tag/save`
- **删除标签**: `DELETE /api/goods-api/tag/deleteById/{id}`
- **分页查询标签**: `POST /api/goods-api/tag/findPage`

## 7. 统计分析服务接口

### 7.1 用户画像分析
- **获取用户画像**: `POST /api/ai-api/statistics/userPortrait`

### 7.2 活跃度分析
- **获取活跃度数据**: `POST /api/ai-api/statistics/activeUsers`
- **获取留存率数据**: `POST /api/ai-api/statistics/retentionRate`

### 7.3 推荐效果分析
- **获取推荐效果数据**: `POST /api/ai-api/statistics/recommendationEffect`
- **获取转化率数据**: `POST /api/ai-api/statistics/conversionRate`

## 8. 系统管理服务接口

### 8.1 菜单管理接口
- **获取用户菜单**: `GET /api/sys-api/sysMenu/userId`
- **获取全部菜单**: `GET /api/sys-api/sysMenu/findAll`
- **保存菜单**: `POST /api/sys-api/sysMenu/save`
- **删除菜单**: `DELETE /api/sys-api/sysMenu/deleteById/{id}`

### 8.2 角色管理接口
- **分页查询角色**: `POST /api/sys-api/sysRole/findPage`
- **保存角色**: `POST /api/sys-api/sysRole/save`
- **删除角色**: `DELETE /api/sys-api/sysRole/deleteById/{id}`

### 8.3 用户管理接口
- **分页查询用户**: `POST /api/sys-api/sysUser/findPage`
- **保存用户**: `POST /api/sys-api/sysUser/save`
- **删除用户**: `DELETE /api/sys-api/sysUser/deleteById/{id}`

## 9. 前端实现要点

### 9.1 流式对话处理
```typescript
// 处理流式AI响应
const handleStreamResponse = async (response: Response) => {
  const reader = response.body?.getReader();
  if (reader) {
    const decoder = new TextDecoder();
    let done = false;
    
    while (!done) {
      const { value, done: readerDone } = await reader.read();
      done = readerDone;
      
      if (value) {
        const chunk = decoder.decode(value, { stream: true });
        // 更新UI显示流式内容
        updateChatContent(chunk);
      }
    }
  }
};
```

### 9.2 身份认证
- 前端通过HTTP拦截器自动添加Authorization头
- Token存储在SessionStorage中
- 请求失败时自动跳转到登录页

### 9.3 错误处理
- 统一错误处理机制
- 用户友好的错误提示
- 自动重试机制

## 10. 性能优化建议

### 10.1 前端优化
- 组件懒加载
- 数据虚拟滚动
- 图片懒加载
- 请求防抖节流

### 10.2 后端优化
- 数据库索引优化
- 缓存策略
- 异步处理
- 连接池配置

## 11. 部署配置

### 11.1 环境变量
- `VITE_APP_BASE_API`: 后端API基础地址
- `VITE_APP_PORT`: 前端服务端口

### 11.2 构建命令
- 前端: `pnpm build`
- 后端: `mvn clean package`

## 12. 常见问题及解决方案

### 12.1 CORS问题
- 后端配置跨域支持
- 前端代理配置

### 12.2 认证失效
- Token自动刷新机制
- 登录状态同步

### 12.3 响应式数据流
- 正确处理WebFlux响应
- 错误传播机制

---

**文档版本**: v1.0  
**更新时间**: 2025年8月  
**作者**: AI推荐云开发团队